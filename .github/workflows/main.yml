name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: 'build-and-push'
    runs-on: ubuntu-latest
  
    steps:
    - name: Checkout
      uses: action/checkout@v4

    - name: Build docker image
      run: docker build -t romualdosantos/nlw.journey.api .


